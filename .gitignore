__pycache__/
*.pyc
*.pyo
*.pyd
.env
.env.*
!.env.example
config.json
*.csv
*.xlsx
.DS_Store
*.command
.venv/
venv/
node_modules/

# Chrome extension signing key — НИКОГДА не коммитить.
# Если потерять — Extension ID сменится и все пользователи должны будут
# переустановить (auto-update через update_url перестанет работать).
extension/key.pem
*.pem

# База данных (локальная)
am-hub-final/data/*.db
am-hub-final/data/*.db-wal
am-hub-final/data/*.db-shm

# Файл кредов
~/.search-checkup-creds.json
*.credentials.json

# Per-agent worktrees (transient)
.claude/worktrees/
